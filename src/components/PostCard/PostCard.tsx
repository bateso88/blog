import React from "react";
// import { Post } from "../../types/posts";
import { Post } from "@Types/posts";

type Props = {
  post: Post; // needs updating
};

const PostCard = ({ post }: Props) => {
  return <div>PostCard</div>;
};

export default PostCard;
